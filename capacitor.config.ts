// vUnified-20260414-national-expansion-v85 — Exact next-steps docs + final store submission verification.
//   v85: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync 1.088s. @capacitor/* installed 8.3.0 (latest: 8.3.1) ✓.
//   Sync ~1s is expected: cap now syncs web assets to BOTH ios/App/App/public + android/assets/public.
//   README-native.md Step H.5 added: exact Xcode install, pod install, JDK 17, ANDROID_HOME, AVD, keystore.
//   Next human steps: (1) Mac App Store → Xcode → pod install → Archive; (2) adoptium.net → JDK 17 → AS.
// vUnified-20260414-national-expansion-v84 — Native scaffold complete + final store submission verification.
//   v84: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync 128ms. @capacitor/* installed 8.3.0 (latest: 8.3.1) ✓.
//   NATIVE SCAFFOLD COMPLETE: ios/ created (57ms) + android/ created (121ms) — npx cap add EXIT:0 both.
//     cap doctor → [success] iOS looking great! 👌 / [success] Android looking great! 👌
//     4 plugins linked: @capacitor/filesystem@8.1.2, @capacitor/share@8.0.1,
//     @capacitor/splash-screen@8.0.1, @capacitor/status-bar@8.0.2 — Package.swift + Gradle ✓
//   Release builds: need Xcode full app (iOS) + JDK 17 + Android Studio (Android) on dev machine.
// vUnified-20260414-national-expansion-v83 — Final App Store / Play Store submission package + parity re-audit.
//   v83: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync 119ms. @capacitor/* all 8.3.0 ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Footer pages parity confirmed — 10 footer routes via InnerPageLayout. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v82 — Final store submission verification + parity re-audit.
//   v82: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync 202ms. @capacitor/* all 8.3.0 ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v82 CONFIRMED — live JSX line numbers re-verified. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v81 — Final store submission verification + parity re-audit.
//   v81: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync 118ms. @capacitor/* all 8.3.0 ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v81 CONFIRMED — live JSX line numbers re-verified. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v80 — Final store submission verification + parity re-audit.
//   v80: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync 131ms. @capacitor/* all 8.3.0 ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v80 CONFIRMED — live JSX line numbers re-verified. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v79 — Final store submission verification + parity re-audit.
//   v79: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync 118ms. @capacitor/* all 8.3.0 ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v79 CONFIRMED — live JSX line numbers re-verified. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v78 — Final store submission verification + parity re-audit.
//   v78: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync 122ms. @capacitor/* all 8.3.0 ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v78 CONFIRMED — live JSX line numbers re-verified. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v77 — Final store submission verification + parity re-audit.
//   v77: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync 178ms. @capacitor/* all 8.3.0 ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v77 CONFIRMED — live JSX line numbers re-verified. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v76 — Final store submission verification + parity re-audit.
//   v76: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync 130ms. @capacitor/* all 8.3.0 ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v76 CONFIRMED — live JSX line numbers re-verified. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v75 — Final store submission verification + parity re-audit.
//   v75: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync 125ms. @capacitor/* all 8.3.0 ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v75 CONFIRMED — live JSX line numbers re-verified. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v74 — Final store submission verification + parity re-audit.
//   v74: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync 128ms. @capacitor/* all 8.3.0 ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v74 CONFIRMED — live JSX line numbers re-verified. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v73 — Final store submission verification + parity re-audit.
//   v73: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync 116ms. @capacitor/* all 8.3.0 ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v73 CONFIRMED — live JSX line numbers re-verified. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v72 — Final store submission verification + parity re-audit.
//   v72: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync 211ms. @capacitor/* all 8.3.0 ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v72 CONFIRMED — live JSX line numbers re-verified. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v71 — Final store submission verification + parity re-audit.
//   v71: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync 156ms. @capacitor/* all 8.3.0 ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v71 CONFIRMED — live JSX line numbers re-verified. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v70 — Final store submission verification + parity re-audit.
//   v70: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync 138ms. @capacitor/* all 8.3.0 ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v70 CONFIRMED — live JSX line numbers re-verified. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v69 — Final store submission verification + parity re-audit.
//   v69: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync 93ms. @capacitor/* all 8.3.0 ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v69 CONFIRMED — live JSX line numbers re-verified. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v68 — Final store submission verification + parity re-audit.
//   v68: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync 131ms. @capacitor/* all 8.3.0 ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v68 CONFIRMED — live JSX line numbers re-verified. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v67 — Final store submission verification + parity re-audit.
//   v67: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync 111ms. @capacitor/* all 8.3.0 ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v67 CONFIRMED — live JSX line numbers re-verified. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v66 — Final store submission verification + parity re-audit.
//   v66: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync 133ms. @capacitor/* all 8.3.0 ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v66 CONFIRMED — live JSX line numbers re-verified. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v65 — Final store submission verification + parity re-audit.
//   v65: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync 124ms. @capacitor/* all 8.3.0 ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v65 CONFIRMED — live JSX line numbers re-verified. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v64 — Final store submission package verification.
//   v64: no config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   build:cap EXIT:0 — 24 static pages, cap sync <150ms. @capacitor/* all 8.3.0 ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v64 CONFIRMED. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v63 — Blanket link reliability fix + secondary-city expansion.
//   v63: LOCAL_FORMS_PART43_MINI (14 entries) + COUNTY_PREFIXES v54 (49 pairs). Total: 3,381+.
//   Link fix: safeOfficialUrl() + 20+ source entries fixed. No Capacitor config changes.
//   store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v63 CONFIRMED. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v62 — State-capital + major-city gap-fill + COUNTY_PREFIXES v53.
//   v62: LOCAL_FORMS_PART42_MINI (14 entries) + COUNTY_PREFIXES v53 (49 pairs). Total: 3,367+.
//   No config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v62 CONFIRMED. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v61 — Tertiary-city expansion + COUNTY_PREFIXES v52.
//   v61: LOCAL_FORMS_PART41_MINI (14 entries) + COUNTY_PREFIXES v52 (50+ pairs). Total: 3,353+.
//   No config changes. store:prepare 11/11 ✅. build EXIT:0 0 warnings. tsc EXIT:0. cap doctor ✓.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   Platform parity AUDIT v61 CONFIRMED. EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v58 — Final store submission package complete.
//   v58: store:prepare 11/11 ✅ EXIT:0. npm run build EXIT:0 0 warnings. build:cap EXIT:0 (102ms sync).
//   npx tsc --noEmit EXIT:0. npx cap doctor EXIT:0 — @capacitor/cli 8.3.0 all ✓ green.
//   ios/ + android/ NOT YET scaffolded: await `npx cap add ios && npx cap add android` on dev machine.
//   No config changes in v58 — production settings from v18 remain correct.
//   store-listing.md v58 final. README-native.md v58 + Step H native build verification.
//   Platform parity AUDIT v58 CONFIRMED (no layout changes v57–v58). EXIT:0 confirmed.
// vUnified-20260414-national-expansion-v90 — Fixed native launch to show custom animated splash + onboarding.
//   ROOT CAUSE: Capacitor WebView loads out/index.html (route "/") on launch.
//   AppSplashOverlay + OnboardingFlow live in /chat (app/chat/page.tsx) and were never reached.
//   FIX 1: app/layout.tsx — synchronous inline <script> in <head> detects window.Capacitor and
//     redirects "/" → "/chat" before React hydrates and before first paint (zero flash).
//   FIX 2: ios.backgroundColor changed from '#ffffff' → '#0B1E3F' (dark navy) so the WKWebView
//     shows the brand color while loading instead of a white flash.
//   FIX 3: android.backgroundColor changed from '#ffffff' → '#0B1E3F' for parity.
//   No changes to splash-screen plugin config — launchAutoHide:false + manual SplashScreen.hide()
//   from nativeInit in chat/page.tsx (v88) remains the correct pattern.
// vUnified-20260414-national-expansion-v88 — Fixed simulator stuck on Apple logo splash screen.
//   Root cause: SplashScreen plugin had launchAutoHide:true but no JS SplashScreen.hide() call.
//   When the plugin was temporarily missing (v86 window), the LaunchScreen.storyboard showed
//   indefinitely. Belt-and-suspenders fix:
//     1. launchAutoHide → false  (JS explicitly controls dismissal; no auto-timer races)
//     2. app/chat/page.tsx ChatPage() calls SplashScreen.hide({fadeOutDuration:300}) in a
//        mount-time useEffect, 150 ms after first render (WKWebView paint completes).
//        StatusBar.setStyle({style:Style.Dark}) is also set for correct nav-bar chrome.
//     3. A 5 s safety timeout in nativeInit() force-hides in case JS hangs unexpectedly.
//   No Swift changes needed — AppDelegate is already correct from v87.
// vUnified-20260414-national-expansion-v55 — Capacitor config (v55 update).
//   v55: Privacy compliance layer complete. store-listing.md v55 (App Store Privacy Nutrition Label,
//   ATT guidance, Google Play Data Safety answers). README-native.md v55 Step G.
//   store:prepare 11/11. 3,259+ LOCAL_FORMS (PART1–PART37). Platform parity AUDIT v55 CONFIRMED.
//   EXIT:0 confirmed. No config changes in v55 — production settings confirmed correct.
// vUnified-20260414-national-expansion-v18 — Production-ready Capacitor config.
//   v18 additions vs v17:
//   - loggingBehavior: 'production' — suppresses verbose Capacitor bridge logs in release builds.
//   - ios.minVersion added (iOS 15.0 — matches Next.js/WKWebView requirements and App Store minimum).
//   - android.minWebViewVersion note added (WebView 96+ for CSS env() safe-area support).
//   - server.errorPath added: custom offline page for network errors in the WebView.
//   - Production build checklist added below for first App Store / Play Store submission.
//
// FIRST TEST BUILD COMMANDS (run after `npx cap add ios` / `npx cap add android`):
//   iOS simulator:  npm run cap:test:ios
//   Android emu:    npm run cap:test:android
//   Full build:     npm run cap:build:ios   (generates unsigned .ipa via Xcode)
//                   npm run cap:build:android (generates .aab via Gradle)
//   See README-native.md for complete first-run instructions.
//
// PRODUCTION CHECKLIST before App Store / Play Store submission:
//   iOS:
//     [ ] Set bundle ID com.regpulse.app in Xcode → Signing & Capabilities
//     [ ] Upload signing certificate + provisioning profile
//     [ ] Set CFBundleShortVersionString + CFBundleVersion in Info.plist
//     [ ] Add NSPhotoLibraryAddUsageDescription to Info.plist (Share → Save to Photos)
//     [ ] Add 1024×1024 App Store icon (no alpha channel) to Assets.xcassets
//     [ ] Set Launch Screen storyboard background to #0B1E3F
//     [ ] Archive → Distribute App → App Store Connect
//   Android:
//     [ ] Set applicationId com.regpulse.app in android/app/build.gradle
//     [ ] Generate release keystore: keytool -genkeypair -v -keystore regpulse.keystore
//     [ ] Configure signing in android/app/build.gradle (storeFile, storePassword, keyAlias, keyPassword)
//     [ ] Add WRITE_EXTERNAL_STORAGE permission (maxSdkVersion="28") if targeting API <29
//     [ ] Set versionCode + versionName in build.gradle
//     [ ] Add 512×512 Play Store icon + feature graphic
//     [ ] Build release: cd android && ./gradlew bundleRelease
//     [ ] Upload .aab to Play Console → Internal Testing track first
// vUnified-20260414-national-expansion-v17 — Final native polish for first test builds.
//   - Filesystem plugin: Directory.Documents (no runtime permission required on Android 10+).
//   - deliverPdf() hardened: split Filesystem/Share error handling, cancellation detection.
// vUnified-20260414-national-expansion-v15 — Initial Capacitor 8.x config.

import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  // ── App identity ────────────────────────────────────────────────────────────
  appId:   'com.regpulse.app',
  appName: 'RegPulse',

  // ── Web assets ──────────────────────────────────────────────────────────────
  // Points to Next.js static export output directory.
  // Generate with: CAPACITOR_BUILD=true next build (sets output:'export' in next.config.ts)
  webDir: 'out',

  // ── vUnified-20260414-national-expansion-v18: production logging ─────────────
  // 'production' suppresses verbose bridge messages in release builds.
  // Set to 'debug' locally when diagnosing WebView/plugin issues.
  loggingBehavior: 'production',

  // ── Server config ───────────────────────────────────────────────────────────
  server: {
    // hostname sets the origin for the in-app WebView (iOS WKWebView / Android WebView).
    // Capacitor default: capacitor://localhost (iOS) / http://localhost (Android).
    // Uncomment `url` during development to load live Vercel deployment instead of local bundle:
    // url: 'https://regbot-landing-page.vercel.app',
    // cleartext: true, // Android only — allow http; remove for production
    androidScheme: 'https',
    iosScheme:     'capacitor',
    hostname:      'app',
    // vUnified-20260414-national-expansion-v18: serve a custom offline page when
    // the WebView cannot reach the server (shown when server.url is set and offline).
    // Only applies when using a live server URL; ignored for local bundle.
    // errorPath: '/offline.html',
  },

  // ── Plugin configuration ─────────────────────────────────────────────────────
  // vUnified-20260414-national-expansion-v93 — All Capacitor plugins removed to achieve a
  // clean Xcode build under Swift 6. Removal history:
  //   v91: @capacitor/share + @capacitor/status-bar (Swift 6 CAPPluginCall.reject errors)
  //   v92: @capacitor/filesystem (Swift 6 LegacyFilesystemImplementation errors)
  //   v93: @capacitor/splash-screen (Swift 6 SplashScreenPlugin / CAPBridgeProtocol errors)
  //
  // Splash UX is now 100% web-layer (zero native plugins required):
  //   1. LaunchScreen.storyboard (dark navy #0B1E3F) — shows while WKWebView loads.
  //   2. AppSplashOverlay.tsx (z-[400]) — animated shield + EKG, dismissed after auth + 2 s.
  //   3. OnboardingFlow.tsx (z-[300]) — Free/Pro/Business tier selection for new users.
  //
  // PDF delivery uses triggerPdfDownload() (web browser download) on all platforms.
  plugins: {},

  // ── iOS-specific overrides ───────────────────────────────────────────────────
  ios: {
    // vUnified-20260428-final-ship-lock: 'automatic' → 'never'.
    // 'automatic' caused WKWebView to apply a native top inset (status-bar height) AND the
    // CSS body paddingTop: env(safe-area-inset-top) applied the same value again — shifting
    // content down ~94px (double-counted safe area). With 'never', the JS/CSS layer controls
    // safe areas exclusively via env(safe-area-inset-*) with no native duplication.
    contentInset:    'never',
    // vUnified-20260414-national-expansion-v90: changed from '#ffffff' → '#0B1E3F'.
    // vUnified-20260414-national-expansion-v93: @capacitor/splash-screen removed — this
    // WKWebView background color is now the only native-layer splash color. It shows while
    // the web content loads, providing a seamless handoff to AppSplashOverlay.tsx.
    backgroundColor: '#0B1E3F',
    // scheme: WKWebView URL scheme for the local web server.
    scheme:          'RegPulse',
    // preferredContentMode: 'mobile' — prevents iPad from scaling as a 2× iPhone view.
    preferredContentMode: 'mobile',
    // vUnified-20260414-national-expansion-v18: minimum iOS deployment target.
    // iOS 15.0 is required for:
    //   - CSS env(safe-area-inset-*) with dynamic viewport units (dvh/svh/lvh)
    //   - WKWebView JavaScript engine improvements needed for React 19
    //   - App Store minimum as of 2025 submission requirements
    // Set IPHONEOS_DEPLOYMENT_TARGET = 15.0 in Xcode → Build Settings (not a Capacitor config key).
    // minVersion: '15.0',  // NOTE: not a valid CapacitorConfig key — set in Xcode Build Settings instead
  },

  // ── Android-specific overrides ───────────────────────────────────────────────
  android: {
    // vUnified-20260414-national-expansion-v90: changed from '#ffffff' → '#0B1E3F' for parity.
    backgroundColor: '#0B1E3F',
    // allowMixedContent: false — enforce https-only for all API calls to Vercel.
    allowMixedContent: false,
    // captureInput: false — do not intercept all input events at the native level.
    captureInput: false,
    // vUnified-20260414-national-expansion-v18: disable WebView debugging in production.
    // Set to true temporarily for Android debug builds (chrome://inspect/#devices).
    webContentsDebuggingEnabled: false,
    // Note: minimum WebView version required: 96+ for CSS env() safe-area support.
    // Modern Android 10+ ships with Chrome WebView 96+. Older devices may need
    // WebView auto-update enabled in Play Store settings.
  },
};

export default config;
